const globalObject = {
  videoSearchResult: [], // keeps track of all the search videos
  videoTrending: [], //keeps track of the trending videos
  searchesPerformed: [], //keep track of all the searchs
  videoCurrentlySelected: [], //Tracks the current selected video and plays it in the modal
};

export default globalObject;
